# Deployed Express API (no trailing slash). When set, EVERY request goes here (not the Vite proxy).
VITE_API_URL=https://medical-rgb5.onrender.com

# When VITE_API_URL is unset, /api and /uploads proxy here (default: same deployed API).
# Local Express only: VITE_DEV_PROXY_TARGET=http://127.0.0.1:5000

# Verify: GET https://medical-rgb5.onrender.com/api/health
# Should include "features":{"adminAds":true,...}. If only {"ok":true}, the old server is still deployed.
